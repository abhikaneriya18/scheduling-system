const UserModel = require('./lib/User')
const AvailabilityModel = require('./lib/Availability')
const BookingModel = require('./lib/Booking')
const BookingLinkModel = require('./lib/BookingLink')

module.exports = {
  UserModel,
  AvailabilityModel,
  BookingModel,
  BookingLinkModel,
}
